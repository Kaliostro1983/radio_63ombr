# Copy to config.env and adjust if needed
APP_NAME=63ombr
DB_PATH=database/radio.db
BACKUP_DIR=backups
BACKUP_KEEP=30

# One-time import sources (paths)
FREQ_XLSX=Frequencies_63.xlsx
# If etalons are in same workbook, leave blank.
ETALON_XLSX=

# Automatic landmark keyword matching: queue new messages + background worker (1=on, default off)
# LANDMARK_AUTO_MATCH=1
